const Alerta = require('../models/Alerta'); const criar = async (req, res, next) => { try { const novoAlerta = await Alerta.create(req.body); return res.status(201).json(novoAlerta); } catch (error) { next(error); } }; const buscarPorSeveridade = async (req, res, next) => { try { const { nivel } = req.params; const alertas = await Alerta.find({ nivelSeveridade: nivel }); return res.status(200).json(alertas); } catch (error) { next(error); } }; module.exports = { criar, buscarPorSeveridade };
