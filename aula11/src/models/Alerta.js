const mongoose = require('mongoose'); const alertaSchema = new mongoose.Schema({ titulo: { type: String, required: true }, descricao: { type: String }, nivelSeveridade: { type: String, required: true, lowercase: true, enum: ['baixo', 'medio', 'alto', 'critico'] }, tags: [String], metadados: { tipoCarga: String, velocidade: Number }, criadoEm: { type: Date, default: Date.now } }); module.exports = mongoose.models.Alerta || mongoose.model('Alerta', alertaSchema);
